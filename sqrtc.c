#include<stdio.h>
#include<math.h>
int main()
{
int n;
float x;
printf("key in the value of n for which square root is found out\n");
scanf("%d", &n);
x=sqrt(n);
printf("The square root of the given number %d is %.2f\n",n,x);
}
